<div class="row">
  <div class="col-md-2">
      <section class=" chef_info center">
        <%= gravater_for(comment.chef, size: 60)%>
      </section>
        </div>
      <div class="col-md-8 well">
        <p><%= comment.description %></p>
        <span class="quite">
          <small>created: <%= time_ago_in_words(comment.created_at)%> ago by
             <%=link_to comment.chef.chefname, chef_path(comment.chef)%></small></span>
      </div>

</div>
